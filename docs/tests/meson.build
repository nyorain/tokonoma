tf16 = executable('f16', 'f16.cpp', dependencies: stage_dep)
test('f16', tf16)

tgraph = executable('graph', 'graph.cpp', dependencies: stage_dep)
test('graph', tgraph)

tfb = executable('fb', 'fb.cpp', dependencies: stage_dep)
test('fb', tfb)
